'use strict';
// const git = require('ufly-git');
const dir = require('./dir');
const exec = require('./exec');

// const name = git.getUser('email');
// console.log(name);

module.exports = {
  dir,
  exec,
};
